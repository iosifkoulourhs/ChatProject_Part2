@model IEnumerable<ChatAppTestCore.Models.Message>

@{
    ViewData["Title"] = "Privacy";
}

<h1>Privacy</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Text)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.When)
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.Sender)
            </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Text)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.When)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.Sender.Id)
            </td>*@
            <td>
                <form asp-action="DeleteMessage" asp-route-id="@item.Id" method="post">
                    @*<a asp-action="DeleteMessage" asp-route-id="@item.Id" data-ajax="true" onclick="return confirm('Are you sure you want to delete User?')">
                        Delete
                    </a>*@
                    <button type="submit" class="btn btn-danger">Delete</button>
                </form>
                
            </td>
        </tr>
}
    </tbody>
</table>
